version: '3.8'

services:
  wechat-api-proxy:
    container_name: wechat-api
    image: ghcr.io/kyeo-hub/wechat-api-proxy:main
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=warn
      # Redis配置
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
    volumes:
      - ./logs:/app/logs
    networks:
      my-network:
        ipv4_address: 192.168.0.177 
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    healthcheck:
      test: ["CMD", "node", "healthcheck.js"]
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 60s

networks:
  my-network:
    external: true
